return {
  -- Keep the buffer/file tab bar visible even with a single buffer open.
  {
    "akinsho/bufferline.nvim",
    opts = {
      options = {
        always_show_bufferline = true,
      },
    },
  },
}
